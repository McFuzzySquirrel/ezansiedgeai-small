<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        eZansiEdgeAI — No INTERNET permission, no dangerous permissions.
        This app is fully offline by design (PRD §10 SP-04).
        Permissions forbidden: CAMERA, MICROPHONE, LOCATION, CONTACTS, INTERNET.
    -->

    <application
        android:name=".EzansiApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ezansi"
        android:installLocation="auto">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Ezansi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

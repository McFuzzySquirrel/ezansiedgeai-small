session_id: ejs-session-2026-04-11-03
author: project-orchestrator
date: 2026-04-11
repo: eZansiEdgeAI/ezansiedgeai-small
branch: feature/gemma4
agents_involved: [project-orchestrator, ai-pipeline-engineer]
decision_detected: true
adr_links: []
tags: [F5.7, device-validation, mediapipe-sdk, api-fix]
refs: []

# Problem / Intent
Continue F5.7 Real-Device Validation: push the Gemma model to the vivo V2434 device, test model loading, and fix any runtime issues discovered during on-device testing. Previous sessions (01, 02) established ADB connection, installed APK, pushed content packs, and downloaded the model file.

# Interaction Summary (Required)

- Human: Asked about using `transformers` Python library to load Gemma 4 E2B model
  - Agent [project-orchestrator]: Clarified that Transformers is for server-side PyTorch inference — we need a `.task` file (LiteRT format) for on-device MediaPipe GenAI SDK. We already have the model downloaded.
  - Outcome: User understood; proceeded with pushing existing model to device.

- Human: Confirmed to proceed with device validation
  - Agent [project-orchestrator]: Pushed `gemma4-1b.task` (529 MB) to device via `adb push` → `run-as cp`. Removed legacy models (Qwen GGUF + MiniLM ONNX). Restarted app and tapped "What are fractions?" suggestion chip.
  - Outcome: Model loading triggered. **Critical bug discovered**: `NoSuchMethodException: setTemperature [float]` — MediaPipe GenAI SDK 0.10.33 API changed.

- Agent [project-orchestrator]: Investigated SDK 0.10.33 API via `javap` on the AAR
  - Outcome: Confirmed API restructuring — `setTemperature`, `setTopK`, `setRandomSeed` moved from `LlmInferenceOptions.Builder` to `LlmInferenceSessionOptions.Builder`. New methods: `setPreferredBackend(Backend)`, `setMaxTopK(int)`.
  - Decision: Fix `GemmaModelProvider.createLlmInference()` to match SDK 0.10.33 API.

- Agent [project-orchestrator]: Applied fix to `GemmaModelProvider.kt`
  - Removed `setTemperature`, `setTopK`, `setRandomSeed` from `createLlmInference()`
  - Added `setPreferredBackend(Backend.GPU|CPU)` via reflection
  - Outcome: Code fix applied, first test — model loaded but native crash during GPU inference.

- Agent [project-orchestrator]: Changed `Backend.GPU` to `Backend.DEFAULT` for auto-detection
  - Outcome: Model loaded in 7.4s (with XNNPack cache), pipeline completed in 43s. Real AI response generated: "Okay! Let's learn about fractions. They are just parts! Like sharing cookies or building something!"
  - **First successful on-device AI inference in eZansiEdgeAI!**

# Agent Collaboration Summary
Which agents participated: project-orchestrator
Key suggestions made: project-orchestrator identified SDK API mismatch via `javap` inspection of AAR
Corrections applied by human: Reminded to maintain EJS journal

# Sub-Agent Contributions
(None yet — direct implementation by orchestrator)

# Agent Influence (Required)
- Suggestions adopted (project-orchestrator): Fix the reflection-based API calls to match SDK 0.10.33 by removing session-level params from model-level options
- Suggestions rejected: None
- Human overrides / corrections: Reminded agent to maintain EJS recording

# Experiments / Evidence
- Pushed model to device and triggered inference → `NoSuchMethodException: setTemperature [float]`
- Used `javap` on `tasks-genai-0.10.33.aar` to enumerate actual SDK methods
- `LlmInferenceOptions.Builder` has: `setModelPath`, `setMaxTokens`, `setMaxTopK`, `setMaxNumImages`, `setPreferredBackend`
- `LlmInferenceSessionOptions.Builder` has: `setTemperature`, `setTopK`, `setTopP`, `setRandomSeed`, `setLoraPath`
- `LlmInference.generateResponse(String)` still exists (uses implicit session with defaults)

# Iteration Log
- Initial model loading attempt failed due to SDK API mismatch
- Investigation revealed temperature/topK/randomSeed are now session-level, not model-level
- Fixed `createLlmInference()` to only set model-level params + preferred backend

# Decisions Made
- Decision: Remove temperature/topK/randomSeed from LlmInferenceOptions and rely on SDK defaults via implicit session
  - Reason: MediaPipe GenAI SDK 0.10.33 moved these params to LlmInferenceSessionOptions. The implicit session used by `generateResponse()` uses sensible defaults.
  - Impact: Model should now load successfully. Generation parameters use SDK defaults rather than explicit configuration. If fine-tuning is needed later, explicit LlmInferenceSession creation would be required.

- Decision: Use `setPreferredBackend(Backend.DEFAULT)` instead of `Backend.GPU`
  - Reason: Explicitly requesting GPU causes native crashes on devices without GPU accelerator support (vivo V2434). Using DEFAULT lets the SDK auto-detect the best available backend (falls back to CPU with XNNPack).
  - Impact: Model loads and inference works correctly on CPU. Second load is faster (7.4s vs 16.6s) thanks to XNNPack weight cache.

# Key Learnings
Technical insights: MediaPipe GenAI SDK 0.10.33 split model-level and session-level config. Model options handle resource allocation (path, max tokens, backend). Session options handle generation behavior (temperature, topK, seed).
Tooling insights: `javap` on extracted AAR classes.jar is the definitive way to check SDK API surface when docs are unclear.

# If Repeating This Work
Do this: Check the actual SDK method signatures via `javap` before writing reflection code
Avoid this: Assuming SDK API stability across minor versions — MediaPipe GenAI is rapidly evolving
Watch out for: The split between LlmInferenceOptions (model-level) and LlmInferenceSessionOptions (generation-level)

# Future Agent Guidance
Prefer: Inspecting AAR/JAR via `javap` to verify reflection targets
Avoid: Hard-coding generation parameters at model load time — use session-level config

## MACHINE EXTRACTS

### INTERACTION_EXTRACT
[{agent:"project-orchestrator", action:"push-model", result:"529MB gemma4-1b.task pushed to device"}, {agent:"project-orchestrator", action:"test-model-loading", result:"NoSuchMethodException: setTemperature"}, {agent:"project-orchestrator", action:"investigate-sdk-api", result:"SDK 0.10.33 API restructured"}, {agent:"project-orchestrator", action:"fix-createLlmInference", result:"removed session-level params, added setPreferredBackend"}]

### DECISIONS_EXTRACT
[{decision:"Remove temperature/topK/randomSeed from LlmInferenceOptions", reason:"SDK 0.10.33 moved to session-level", impact:"Model loads; uses default generation params"}, {decision:"Add setPreferredBackend for GPU/CPU", reason:"Official SDK 0.10.33 API", impact:"Proper backend selection"}]

### LEARNING_EXTRACT
[{insight:"MediaPipe GenAI 0.10.33 splits model-level and session-level config", source:"javap on tasks-genai-0.10.33.aar"}, {insight:"LlmInference.generateResponse() uses implicit session with defaults", source:"javap class inspection"}]

### AGENT_GUIDANCE_EXTRACT
[{prefer:"javap AAR inspection for verifying reflection targets", avoid:"assuming SDK API stability across versions"}]

### SUB_AGENT_EXTRACT
[]
